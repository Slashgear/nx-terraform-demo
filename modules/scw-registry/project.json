{
  "name": "scw-registry",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "modules/scw-registry",
  "targets": {
    "fmt": {
      "executor": "nx:run-commands",
      "options": {
        "command": "terraform fmt -check",
        "cwd": "modules/scw-registry"
      },
      "configurations": {
        "fix": {
          "command": "terraform fmt",
          "cwd": "modules/scw-registry"
        }
      }
    },
    "validate": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["terraform init -backend=false", "terraform validate"],
        "cwd": "modules/scw-registry",
        "parallel": false
      },
      "dependsOn": ["fmt"]
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tflint --init && tflint",
        "cwd": "modules/scw-registry"
      },
      "dependsOn": ["validate"]
    },
    "security": {
      "executor": "nx:run-commands",
      "options": {
        "command": "checkov -d . --quiet --compact",
        "cwd": "modules/scw-registry"
      },
      "dependsOn": ["lint"]
    },
    "docs": {
      "executor": "nx:run-commands",
      "options": {
        "command": "terraform-docs markdown table --output-file README.md --output-mode inject .",
        "cwd": "modules/scw-registry"
      }
    }
  },
  "tags": ["terraform", "infrastructure", "registry", "docker"]
}
